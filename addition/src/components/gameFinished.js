import React from 'react'

const gameFinished = () => {
    return (
        <div>
            <h1>Game Finished!</h1>
            <button className='btn btn-primary'>Click Me</button>
        </div>
    )
}

export default gameFinished
